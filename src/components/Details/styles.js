import { makeStyles } from '@material-ui/core';

export default makeStyles(() => ({
  income: {
    borderBottom: '10px solid rgba(0, 0, 255)',
  },
  expense: {
    borderBottom: '10px solid rgba(225, 0, 0)',
  }
}));